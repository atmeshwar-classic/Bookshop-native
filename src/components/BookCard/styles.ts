import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
  name: {
    fontWeight: 'bold'
  },
  item: {
    padding: 20,
    marginTop: 10,
    borderRadius: 10,
    backgroundColor: 'white',
    marginHorizontal: 5,
},

totalAmout: {
  padding: 20,
  marginTop: 10,
  borderRadius: 10,
  fontWeight: 'bold',
  fontSize:30,
  marginHorizontal: 5,
}
});